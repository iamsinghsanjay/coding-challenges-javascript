# coding-challenges-javascript
### Javascript Coding Challenges

Reverse an array without using reverse().
Check if an array is a palindrome.
Find the maximum difference between any two elements.
Find the sum of elements at even indices only.
Rotate an array left by 1 position.
Rotate an array right by 1 position.
Move all negative numbers to the beginning (maintain order).
Move all zeros to the end (maintain order).
Count how many numbers are greater than a given value x.
Replace every element with its square if it’s even, cube if odd.
Find the first repeating element.
Find the first non-repeating element.
Check if two arrays are anagrams (same elements, different order).
Remove duplicates without using Set.
Find the second largest element (single pass preferred).
Find the index of the first unique element.
Swap first and last elements without using extra array.
Count pairs with sum = target (simple brute force).
Flatten a 1-level nested array (e.g., [1, [2,3], 4]).
Find the difference between sum of even and odd numbers.
Create a new array where each element is the sum of neighbors.
Find the common elements between two arrays.
Check if an array is sorted or not.
Remove all falsy values (false, 0, "", null, undefined, NaN).
Find the minimum distance between two given values.

Rotate array by k positions (optimize for large k).
Find all pairs with given sum (avoid duplicates).
Find the longest increasing subarray.
Find the maximum subarray sum (Kadane’s).
Find equilibrium index (left sum = right sum).
Find majority element (> n/2 times).
Merge two arrays and remove duplicates + sort.
Find the missing number (1…n) efficiently.
Find duplicate elements in O(n).
Partition array into evens and odds (in-place).
Find subarray with given sum.
Count subarrays with sum = k.
Find the longest subarray with unique elements.
Find intersection of 3 arrays.
Sort an array of 0s, 1s, and 2s (Dutch flag).
Find the next greater element for each element.
Find the maximum product of two elements.
Rearrange array in zig-zag fashion.
Find the smallest subarray with sum ≥ target.
Replace each element with the greatest element to its right.
Find leaders in array (greater than all to the right).
Find all triplets with sum = target (brute).
Find the first missing positive number.
Count inversions in array (basic approach).
Find the minimum number of swaps to sort array.

Solve 3Sum (optimized).
Solve 4Sum problem.
Find maximum product subarray.
Find longest consecutive sequence (O(n)).
Sliding window: max sum of size k.
Sliding window: longest substring with k distinct.
Find median of two sorted arrays.
Trapping rain water problem.
Container with most water.
Find kth largest element (Quickselect).
Merge k sorted arrays.
Find subarray with maximum XOR.
Count subarrays with XOR = k.
Find maximum sum rectangle (2D array).
Find longest bitonic subarray.
Find peak element in array.
Search in a rotated sorted array.
Find minimum in rotated sorted array.
Find rotation count of array.
Find all duplicates in O(1) space.
Rearrange array in-place (index = value).
Solve jump game (can reach end?).
Minimum jumps to reach end.
Find subarray with equal 0s and 1s.
Find longest repeating character replacement.
Find minimum window subarray.
Find sliding window maximum.
Implement LRU cache using array logic.
Count smaller elements to right.
Find k closest elements to target.
Find maximum circular subarray sum.
Rearrange array in wave form (optimal).
Find all permutations of array.
Find next permutation.
Find previous permutation.
Detect cycle in array (Floyd's algo).
Find duplicate using cycle detection.
Longest subarray with sum divisible by k.
Split array into k subarrays minimizing largest sum.
Count range sum queries efficiently.
Find subarray with smallest variance.
Compute prefix & suffix product arrays.
Product of array except self (no division).
Find max chunks to sort array.
Find minimum swaps to group elements ≤ k.
Find longest subarray with sum ≤ k.
Solve stock buy-sell max profit variations.
Find maximum difference with constraints.
Find minimum subarray to remove to make sorted.
Design array-based data structure with O(1) ops (insert/delete/random).